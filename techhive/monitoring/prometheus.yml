global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files: []

scrape_configs:
  - job_name: "techhive-backend"
    metrics_path: /metrics
    static_configs:
      - targets:
          - backend:5000
